rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application data is accessed through the trusted Express server.
    // Client-side Firestore access is intentionally disabled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
